import React, {
  View,
  StyleSheet,
  PropTypes
} from 'react-native'

import BrowseNavigationBar from '../browse/BrowseNavigationBar'

// this will change for sure so manage them later for now just use browse nav bar
// TODO Remove this if not changed
//const FavouriteWorkoutNavBar = (props) => {
//  return (
//    <View style={ styles.container }>
//    </View>
//  )
//}
//
//FavouriteWorkoutNavBar.propTypes = {}
//const styles = StyleSheet.create({
//  container: {}
//})

export default BrowseNavigationBar
