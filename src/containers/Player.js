import React, {
  View,
  StyleSheet,
} from 'react-native'

import VideoScreen from "../components/Player/VideoScreen"

const Player = (props) => {
  return (
    <VideoScreen />
  )
}

export default Player